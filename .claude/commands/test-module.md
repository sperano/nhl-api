---
description: Run tests for a specific module (e.g., /test-module date)
---

Run tests for the {{module}} module using `cargo test --lib {{module}}::`.

If tests fail, analyze the failures and suggest fixes. Show the test output and explain what went wrong.
